<!DOCTYPE html>
<html>

  <head>
  </head>

  <body>
    <nav class="navbar navbar-expand bg-body-tertiary fixed-top ">
      <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <%= link_to 'TABELA', root_path, class: "nav-link"%>
            </li>
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="#">JOGOS</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container-fluid" style="margin-top:100px;">

      <h2 style="font-style:italic;text-align:start">JOGOS AGENDADOS</h2>

      <div class="row">
      <% @match_data_scheduled.each do |match| %>
        <% if match.status === "agendado" %>
            <div class="col-sm-6 mb-3 mb-sm-0">
              <div class="card mb-4">
                <div class="card-body">
                  <div class="d-flex flex-column flex-sm-row align-items-center mb-3">
                    <img src=<%= asset_path('logo_brasileirao.png') %> style="height:50px;">
                    <span class="title-card-brasileirao flex-grow-1 text-center text-sm-start text-lg-start">BRASILEIRÃO BETANO</span>
                    <span class="card-match-status ms-sm-auto mt-2 mt-sm-0"><%= match.status %></span>
                  </div>
                  <div class="d-flex flex-md-row flex-column justify-content-center align-items-center px-4 text-wrap">
                    <div class="d-flex flex-column align-items-center justify-content-center gap-2 w-100">
                      <img src="<%= match.escudo_mandante %>" style="height:60px;">
                      <span class="text-center"><%= match.nome_mandante %></span>
                    </div>
                    <div class="d-flex flex-column align-items-center fs-1 pb-4 mx-4 mt-2 flex-shrink-0 w-auto ">
                      <span class="fs-4 fw-bold"><%= Date.strptime(match.data_jogo, "%d/%m/%Y").strftime("%d/%m") %></span>
                      <span class="fs-5"><%= match.hora_jogo %></span>
                    </div>
                    <div class="d-flex flex-column align-items-center justify-content-center gap-2 w-100">
                      <img src="<%= match.escudo_visitante %>" style="height:60px;">
                      <span class="text-center"><%= match.nome_visitante %></span>
                    </div>
                  </div>
                  <div class="d-flex flex-column flex-md-row justify-content-between text-center mt-3" style="color:rgba(3, 3, 3, 0.5);">
                    <span><%= match.estadio %></span>
                    <span><%= match.rodada %></span>
                  </div>
                </div>
              </div>
            </div>
        <% end %>
      <% end %>
      </div>

      <h2 class="mt-3" style="font-style:italic;text-align:start">JOGOS FINALIZADOS</h2>

      <div class="row">
      <% @match_data_ended.each do |match| %>
        <% if match.status === "finalizado" %>
            <div class="col-sm-6 mb-3 mb-sm-0">
              <div class="card mb-4">
                <div class="card-body">
                  <div class="d-flex flex-column flex-sm-row align-items-center mb-3">
                    <img src=<%= asset_path('logo_brasileirao.png') %> style="height:50px;">
                    <span class="title-card-brasileirao flex-grow-1 text-center text-sm-start text-lg-start">BRASILEIRÃO BETANO</span>
                    <span class="card-match-status ms-sm-auto mt-2 mt-sm-0"><%= match.status %></span>
                  </div>
                  <div class="d-flex flex-md-row flex-column justify-content-center align-items-center px-4 text-wrap">
                    <div class="d-flex flex-column align-items-center justify-content-center gap-2 w-100">
                      <img src="<%= match.escudo_mandante %>" style="height:60px;">
                      <span class="text-center"><%= match.nome_mandante %></span>
                    </div>
                    <div class="d-flex flex-column flex-md-row gap-3 gap-md-3 fs-1 align-items-center">
                      <span><%= match.placar_mandante %></span>
                      <img src="<%= asset_path('x.png') %>" style="width:15px;">
                      <span><%= match.placar_visitante %></span>
                    </div>
                    <div class="d-flex flex-column align-items-center justify-content-center gap-2 w-100">
                      <img src="<%= match.escudo_visitante %>" style="height:60px;">
                      <span class="text-center"><%= match.nome_visitante %></span>
                    </div>
                  </div>
                  <div class="d-flex flex-column flex-md-row justify-content-between text-center mt-3" style="color:rgba(3, 3, 3, 0.5);">
                    <span><%= match.estadio %></span>
                    <span><%= Date.strptime(match.data_jogo, "%d/%m/%Y").strftime("%d/%m") %></span>
                  </div>
                </div>
              </div>
            </div>
        <% end %>
      <% end %>
      </div>
  </body>
</html>